services:
  glances:
    image: nicolargo/glances:latest
    ports:
      - 61208:61208
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro,z
      - /etc/os-release:/etc/os-release:ro,z
    environment:
      - GLANCES_OPT=-w
    pid: host
    restart: unless-stopped
